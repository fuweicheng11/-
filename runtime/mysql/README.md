# MySQL 配置说明

本目录在源码仓库中仅保留数据库配置骨架，不包含本地数据库数据文件。

运行项目前，请至少完成以下一项：

1. 编辑 `app_db.json`，填入可用的 MySQL 连接信息
2. 或使用环境变量覆盖数据库连接参数

源码仓库不包含：

- `runtime/mysql/data/`
- `runtime/mysql/logs/`
- `runtime/mysql/tmp/`

如果需要恢复某台机器上的本地数据库数据，请从单独的备份包中覆盖还原，而不要依赖本仓库。
